/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // opcional pero recomendado
  // Puedes agregar otras configuraciones válidas aquí
}

export default nextConfig
